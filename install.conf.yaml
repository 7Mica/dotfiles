- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: zshrc
    ~/.vimrc: vimrc
    ~/.oh-my-zsh/custom/themes/bullet-train.zsh: oh-my-zsh/themes/bullet-train

- shell:
    - [git submodule update --init --recursive, Installing submodules]
